i#include "main.h"
#include <stdio.h>

/**
 * main - returns 1 if true else return 0
 * @c: check if uppercase.
 *
 * Return: Always 0.
 */
int main(void)
{
    char c;

    if (_isupper(c))
    {
	    return (1);
    }
    else
    {
	    return (0);
    }
}
